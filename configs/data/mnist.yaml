shared:
  _target_: src.data.torchvision_dataset.TorchvisionDataset
  data_dir: ${paths.data_dir}
  dataset:
    _target_: torchvision.datasets.MNIST
    transform:
      _target_: torchvision.transforms.Compose
      transforms:
        _target_: torchvision.transforms.ToTensor

train:
  train_kwargs:
    train: True

val:
  train_kwargs:
    train: False
